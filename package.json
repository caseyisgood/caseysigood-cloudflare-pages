{
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "deploy": "npm run docs:build && gh-pages -d dist -t true"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "vitepress": "^1.6.3"
  },
  "dependencies": {
    "@amap/amap-jsapi-loader": "^1.0.1"
  }
}
